"""Canonical home of the Jeles persona.

The persona JSON (`jeles_persona.json`) lives here as package data. Load it
via the top-level `jeles.load_persona()` helper rather than reading the file
directly, so consumers don't hardcode the path.
"""
